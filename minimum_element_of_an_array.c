#include<stdio.h>
int main()
{ 
    int N,i; 
    scanf("%d",&N);
    int a[N];
    int max=-9999;
    for(i=0;i<N;i++) 
    {
        scanf("%d",&a[N]);
        }
        if(max<a[i])
{
    max=a[i];
    }
    printf("%d",max);
    
}